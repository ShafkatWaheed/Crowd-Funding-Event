# FastAPI & server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0

# Config & validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Auth (Firebase Admin SDK for token verification)
firebase-admin>=6.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# Email (SendGrid — default provider; swappable via EMAIL_PROVIDER)
sendgrid>=6.11.0

# Ticket QR encryption (AES-256-GCM)
cryptography>=42.0.0

# Excel export (schedule)
openpyxl>=3.1.0

# Migrations
alembic>=1.13.0
